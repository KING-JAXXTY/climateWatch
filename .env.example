# Frontend environment variables
# Copy to .env.local. Keep keys private.
# This file is for documentation only - DO NOT commit real keys!

# API base URL
# Local dev: value is not needed (fallback hardcoded to http://localhost:5000/api)
# Vercel production: set to /api (same-domain routing via vercel.json)
VITE_API_URL=/api
